---
applications:
- name: kwetter-ui-jhen
  memory: 64M
  disk_quota: 64M
  instances: 1
  random-route: true
  buildpack: staticfile_buildpack
  path: /dist/
  env:
    FORCE_HTTPS: true

#Make Staticfile -> pushstate: enabled
